'use strict';

describe('myApp.userLogin module', function() {

    beforeEach(module('myApp.userLogin'));

    describe('userLoginCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var userLoginCtrl = $controller('userLoginCtrl');
            expect(userLoginCtrl).toBeDefined();
        }));

    });
});